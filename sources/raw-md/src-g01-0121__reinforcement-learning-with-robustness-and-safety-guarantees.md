> Source: https://www.youtube.com/watch?v=kQ7X0pfF_0U

so let me start so today I'm going to talk about Rob reinforcement learning with a robustness and safety guarantees right so I'm sure that all of you um hello here here are familiar with reinforcement learning at some level uh some of you may be doing your Accu research on green personal learning on our Theory algorithms or even application of RL into specific problems of Interest reinforcement learning is a class of machine learning focusing on how to learn the optimal sequence of action in an unknown and evolving environment in order to maximize the cumulated long-term Rewards so this is the canonical diagram that we typically use to represent reinforcement learning Paradigm a learning algorithm interacts with an environment by taking a sequence of action the learning algorithm gets the state observations and the reward and the goal of the learning algorithm is to maximize the cumulative long-term reward as an example consider the problem of learning to play at argument the actions can come from the joysticks the environment is a diary game engine and the screen images are the observation and the rewards are the actual points in the game in another example the actions can come from the steering wheel to the environment of an autonomous vehicle and the observations can be the camera or lidar images green personal learning had some shining successes in the past few years in 2015 defense published this interesting receiver of a deep parallel algorithms that is able to outperform human agents in playing a daddy game this was followed by a series of interesting deep parallel algorithm from deepmind including the impressive alphago algorithms and its variants which were able to outperform human needs actually human experts in much more complicated games such as coaches and xiaomi ARL also had some interesting successes in the robotics domain for example this work from open a in 2019 got much press coverage at that time and they used an RL algorithm for a robotic hand to solve the Rubik's Cube problem and there are other impressive news for the article including the chip placement design but from Google and recommendation system work from Netflix and Microsoft but if you carefully read the success stories you will immediately realize that most of these oral success stories are limited to very structured or simulated environment such as games of simple robotic setting in fact the success stories of reinforcement learning in real world Engineering Systems are rather Limited so what is so what is holding up reinforcement learning from emerging as the go-to solution for the control of real world Engineering Systems so I believe that one of the main reasons is the fragility of reinforcement learning algorithm in the sense that this algorithm lack robustness and safety guarantees so anyone who has trained in our algorithm must be familiar with the scenarios like this in the music environment simulation here a hawker is falling repeatedly during uh training and sometimes even during testing it is natural to consider a scenarios happening in the real world where a robot controlled by an oral algorithm falling down while executing its task in fact if you read the fine print you will realize that this work which got much press coverage at that time was successful only 32 percentage of the time and of course namely using any mlr algorithm uh without any safety or robustness guarantees can lead to catastrophic outcomes in the real world engineering system as we have seen in a series of bad examples recently so how do we develop scalable real personal learning algorithm that are probably robustancy this is one of the fundamental question at the core of my research effort in the past couple of years and today I will try to present some of my recent results on addressing these fundamental questions so first I will talk about robustry in personal learning so I'll talk about robust treatment learning uh we actually have a series of work on the topic of rubber stream Postman learning from our group in today's talk I will mainly focus on this particular work which was presented in new risks um two months back uh this is a joint work with my PhD students is the lead order in this series software and for this particular work we've got this wonderful opportunity to collaborate with Dr Muhammad Sade from Google research but before discussing my algorithm on robust reinforcement learning let's ask ourselves why do we need robust reinforcement learning the key reason is the simulation to reality reimbursement learning algorithm typically learn a policy by training on a simulator however the real world system parameters can be different from that our Simulator for example in robotics the mass the friction sensor noise action delays Etc can be different between the real world system and the simulator which is trying to approximate the real world system and it is known both in theory and practice that RL algorithms sprayed on a simulator May perform too early in the real world system due to this simulation to reality Gap and is not a hypothetical question this actually a system and it's not a hypothetical question it's actually a serious problem that is being actively addressed both by the academy as well as the industry you will find a number of papers seminars workshop on addressing this problem from Academia as well as some interesting recent efforts on the industry on addressing this product in fact it is recognized as one of the most important challenges that we need to overcome to make RL algorithm Deployable in the real world Engineering Systems so in this context I address the following problem how do we develop scalable reinforcement learning algorithms that are probably robust against parameter mismatches between the simulator model and the real world model so our standard in RL I'll use the formalism of Marco decision process to model this problem I am assuming that all of you are familiar with mdp somewhat so I'll be very brief here not not to bore you with the notation so in an MTP we have a set of States a set of action a probability transition function B so P of X Prime given s comma a represent the probability of moving from State s to S Prime if action a is taken the p is also called the model of the system so for the rest of the talk I will use the keyword model instead of probability transition function there is a reward function associated with each state action pair and a control policy is a mapping from the state space to the probability distribution over the action space and MTP is a very useful and a general formalism that can capture most of the real world control problem of interest for example in this robotics control problem the position of the robot can be the state the action can be the force or tors that we apply to move the robot and the reward can depend on the poster or the Gate of the robot and at any given time any given time t a control policy file selects an action 80 given the current state SD and the next state St Plus One will be realized according to the model of the proportion the value of a policy is the expected accumulative disorder reward that we can get by following that policy and optimal value function is the maximum value of policy can achieve and the optimal policy is the policy which achieves at maximum value and the mdp question is the following how do we find the optimal policy when the model is known and we know that dynamic programming provides an answer to this question for example you can use value iteration or policy iteration for finding the optimal policy reinpersonal learn is the learning version of this mdp problem that is how do we learn the optimal policy when the model is done so in this part of the talk and I will focus mainly on offline reinforcement learning problem and offline rubbish reinforcement learning problem here also the goal is the same how do we learn the optimal policy when the model is unknown but using only historical offline data without having access to any direct interaction with the environment or with a sophisticated simulator model so offline RL is a very promising Paradigm for many real world applications such as autonomous vehicle recommendation system power system clinical treatment Etc but it's typically easy to get historical data but it is very difficult to model the whole system and expensive or even unsafe to have direct interaction with the real world system for example in power system you may be able to get operational data but definitely not advisable to have real world interaction with the public system for the learning purpose so whenever we say learning in the context of machine learning it means that there is some data to learn from where does the data come from in reinforcement learning context in the offline RL as I just mentioned we assume that the learning algorithm has access to a set of data generated according to your behavioral policy from a nominal model so our offline data is represented by Script D which is a double State action reward on the next state and the next state is generated according to this model and most online RL algorithm learned optimal policy by explicitly or even implicitly learning about the underlying model but what if the models is wrong in this particular context what is the nominal model which generated the data is different from The Real World model on which you want to deploy the system as I mentioned before the simulation to reality Gap is a fundamental Challenge and arguably one of the most important challenges that we need to overcome to make our RL algorithm Deployable in the real world setting so I use the foreign to solve this problem a robust mdp is similar to a standard MTV in the sense that it has the same state space action space and the reward function excuse me but this difference because we consider a set of model instead of a single nominal model so the set of model is script set or model we did not absolute p is also called the uncertainty set so the uncertainty set is defined as a Cartesian product of the uncertainty sets corresponding to each state action layer script PSA now the uncertainty set corresponding to a given State action pair can be thought as a board of radius row around the nominal probability distribution with respect to that particular State action pair so that rho is the radius of uncertainty and the D is a distance metric between two probability distributions such as TV chi-square RTL now the robust mdp objective is the following given the uncertainty said how do we find the optimal robust policy as a solution of this optimization problem so you want to find the optimal robust policy Arc Max over Pi Min over all possible models in the uncertainty set and expectation of the expected discounted cumulative reward where the expectation is taken with respect to that particular model from the uncertainty set the intuitively we want to find the maximum policy another way to think about is by defining the raw bash value function so I can Define the robust value function V by as mean over T and V Pi P so essentially the value correspond value of a policy corresponding to the worst possible model and our goal here in the robust mdp is to find the policy which will maximize the robust value function so there is a one-to-one correspondence between the standard mdp objective and robust mdp objective in the standard mdp objective you want to find a policy that maximize the value function here you want to find a policy that maximize the robust value function any questions now robust Donald problem is the learning version of the robust mdp problem that is how do we learn the optimal robbers policy when the uncertainty set is unknown so where does the data come from in the Raw basharl problem here in the offline robustrial context we make the same standard data assumption that the use in the non- robust oral context that is we are going to assume that the algorithm has access to the offline data generator according to behavioral policy from a nominal simulator model so my data opening data script B which is the Tuple State action reward and the next date the next state is generated according to the nominal model P naught so I emphasize that's a nominal model P naught which may be different from The Real World model so this immediately brings out one of the many challenges in robust Turtle so if you look at this equation uh the octave to compute optimal robust policy you may have to evaluate this expectation with respect to all the possible models in the uncertainty set but you have the data only from the nominal model clearly you cannot generate the data according to each and every models in the uncertainty set you have to leave with the data that you have so how do we solve this problem it's one of the challenges that we need to overcome so before before I describe the solution approach let me quickly summarize the related work in the slide so the raw best mdp formulation was first proposed and developed in two seminal parallel work by gurudayenger and Lily mandelgoy one from Berkeley and one from Stanford and there was a series of work on studying robust mdp but most of his work uh was on the series of workers on planning program but not on the learning problem of course RL Community addressed this problem from the learning contest but our standard standard in the literature the initial Works were mostly on the tabular setting on linear function approximation it's getting which may not scale to large State space there are obviously deep parallel algorithms which is trying to solve this uh robust MTP problem but our standard in the Deep parallel with the equator most of these algorithms are based on heuristics without provable guarantees I acknowledge that this is only a partial list uh robust learning is a very broad area here I included Only The Works which essentially use the robust mdp formulation for example in another stream of work there is robust control literature which is studying this problem but I just focused on this here topologies haven't missing anything important so in this context our main contribution is the following we address the offline professional problem with very large State space using function approximation with provable guarantees on the performance of the learn policy quick question yes um regarding if you go back to the previous slide please regarding the learning problem in robust mdp can you give us an idea of how the solutions look like regarding the planning problem only not with data thank you um I that is indeed what I'm going to talk about in this particular slide okay thanks so before before discussing the learning problem let's first understand how do we solve the planning problem right so that how do we find Optimal robbers policy if the uncertain reset is known this can be done by robust dynamic programming in fact we can actually Define a robust Belmont operator T which is a robust version of the standard Wellman operator so the rubbish Wellman operator operating on Q can be defend the following way R plus gamma times in forward all possible models in uncertainty set and an expectation with respect to that particular model so you can realize that if there is no in fear this is the standard Bellman operator so this is when with the Empower all possible model this is the rubbish Wellman operator but we can show that many of the useful properties of the Bellman operator carry over to the robust Wellman operator in particular we can show that t is a contraction operator and so there's a unique fixed point so we can actually defend the robust Q iteration I can Define q k plus one excuse me I can Define q k plus 1 as tqk and since T is contraction q k will converge us to Q star and the optimal robust policy can be computed as a r Max policy so this is how we solve the robust dynamic programming but our goal is the learning problem so how do we solve the robust oral problem using offline data for rmdps with very large State space and I propose an algorithm called robust treated cubrication algorithm to solve this problem okay so the basic idea behind our robust treated queue iterational algorithm is straightforward I want to approximate the exact rqi update with function approximation using offline data so I want to get the next great q k plus one as a solution of the solution of an optimization problem or a function class script F script f is the class that I am considering for representing the robust Q value function intuitively I am trying to get the best fit for tqk by minimizing the square error while this might seem simple straightforward it is particularly challenging the robust oral context mainly because evaluating Robert Spellman operator with function of function approximation using offline data is challenging to see one of the challenges let's recall the robust Wellman operator as I mentioned before there's an in power all possible models here so solving that in problem itself is difficult and there is an expectation so estimating that expectation using the data only from the nominal model is also very challenging so in our work we propose to overcome this particular challenge by a dual reformulation of the robish Wellman operator so let me state that in the form of a proposition so let's consider an rmdp return certainty sets with T so here I am going to restrict the distance measure to only total variation distance for defining the uncertainty set then the robust Belmont operator can be equivalently written in the following form so there's a lot of notation let me unpack that for you there are two takeaway messages here the first thing that you can notice that the expectation over all possible model is now replaced with respect to expectation over only the nominal model the objective is different but even though the objective is different since the expectation is only with respect to the nominal model you may be able to estimate that using the offline data the second thing to notice is that the infimum or the minimization over all possible model is now replaced by a scalar minimization over ETA and then we can show that that inner optimization problem is convectionated so this kind of dual reformulation techniques is known in the distribution robust optimization literature we essentially borrow that idea and apply that in the context of robust dynamic programming but this formulation alone will not solve our issue because here if you write it in the Dual reformulation you have an inner optimization problem to solve so let me rewrite this inner optimization problem in the next slide looks like this so you can see that there is an expectation outside there's an epimer inside so that still requires solving an optimization for each state action pair which is clearly infeasible for large MDB so here also we have a simple straightforward idea to overcome this so we want to reformulate this problem as a functional optimization problem instead of solving it as a multiple scalar optimization problem so for that I'm going to define a dual loss function so L divided the Dual loss function for the function G for a given Q function is different in the following way here also there is a lot of notation but one key thing to notice here is that equation 1 and equation two the objective in the black phone are identical except that is ETA in equation 1 is replaced by G evaluated at s and a otherwise they are identical but they are different because in equation one there is a scalar in premium for each state action pair and the infimum is inside the expectation here the infimum is outside the expectation but that's a functional info functional optimization problem or an appropriate function space okay now can I solve the equation 2 instead of equation 1 because we are exchanging in expectation and minimize minimization with appropriate function class for example if your function class is Rich enough you can show that you can do that even if the state space is continuous so you can interchange the expectation and minimization so instead of solving the multiple scalar optimization problem in equation one I am going to solve equation 2 our functional optimization problem so for any given queue I can solve this L dual function all of the appropriate function class to get the appropriate dual function I will due to time constraint I will skip over the other details of our QA algorithm I'll just jump to the algorithm so in RP algorithm I start with an offline data set and I have two function class script depth script f is a function class that I use to represent the the robust Q value function script G is the function class that I am going to represent uh using to represent the Dual variable function RFQ algorithm operates in iterate in the first step of each trade I perform the Dual variable function optimization for a given QE trade qk so we compute the Dual variable function corresponding to q k by solving the same optimization problem that I displayed before but that optimization problem was with respect to the true expectation we don't have true expectation but we have the data offline data so you can use the offline data to estimate that function so and we can use the empirical rules minimization approach to solve that problem this will indeed introduce some error but we can get an upper Bound for that error okay so what will we do so given a QT I get the dual variable function for that qk now given the Dual variable function I get the next iterate q k plus 1 by solving the least Square problem which I presented earlier now this is the exact equation for that least Square problem again least Square problem requires like an excite expectation we don't have the exact expectation but we can again we can again use the offline data to get an empirical estimate about this Lee Square problem here also there will be some error we can appropriately bound that error and this continues for capital K number of iterations yes uh so we don't so here we don't exactly calculate the computational complexity because uh we are we are considering a very large State space so we are just treating it as an empirical list minimization problem so my goal is to characterize the sample complexity and computational computational complexities okay so now we have an algorithm what is a performance guarantee I can show the performance guarantee under some standard assumption so I'm not going to explain this assumption in detail I'm just going to mention that these are the standard assumptions that you will see in non robust offline oral literature also so essentially using the same assumption that we use in the non-rober stop linear literature we can give the performance guarantee for the robust R so the first assumption constant probability essentially implies that the offline data has some some coverage as a standard assumption in the non-robus of linear literature approximate completeness roughly means that the function class that you represent to use to represent the Q value function is Rich enough and in this particular case I propose the natural extension of assumptions too essentially I'm assuming that the function class that I am going to represent using to represent the Dual variable function is also original so using the same standard assumption as in the non-robestop in our literature I can give the following performance guarantee so let Pi K be the output of our RQ algorithm then I can get the following Bond the robust value of the optimal robust policy minus the robust value of the output Pi K can be upper bounded by these two terms so let's look at the second term there is an Epsilon C and Epsilon dual that is coming from approximate completeness and approximate dual realizability so if it is small enough so you can neglect that term it's an RC actually a gamma of our K so if you have K large enough the number of iteration is large enough again you can neglect the second term so the first term is important term and you can see that that is of the order of 1 over square root n and low cardinality of the function class okay so this is a standard statistical learning uh bound that you get in many machine learning result including in the non-proper stop linear so essentially what I'm trying to emphasize is that using the same standard assumption as in the non-drowverstock lane RL we can give a performance guarantee for the robust RL algorithm okay so now we have a theoretical performance guarantee but is it good does it work in practice right so we did Excellency evaluation in mujoko environment so I'm going to show the mujoko evaluation excuse me for interrupting I have I have a question in your previous result please yes um it seems that their left hand side is an expectation over the initial state does the right hand side have any dependence on d0 um so we so that dependence is kind of captured and um uh concentrability which I am see got it yes I see thank you yes no um I think it is Cube yeah okay yeah done it's good um I think it's I'm pretty sure that's Cube but uh let's let's chat up thank you I don't know about the recent result that's in the last one half a year or so but when we were referring it was Cube sorry okay so coming back what is the performance how does it work in practice how does it work in simulation so here I'm going to show you the performance evaluation in the hopper simulation environment uh in this plot I'm evaluating two algorithm the red line is the fqi algorithm the standard non-robust algorithm featured Cube iterational algorithm the black curve is our algorithm the robust version of that standard fqi algorithm y-axis is the average cumulative reward by in 20 trials it's kind of performance evaluation x-axis is where I'm introducing the simulation to reality Gap so by kind of introducing by changing this parameter joint friction loss in the hopper environment uh the value 0 means that there is no simulation to reality Gap and as I increase x value there is more and more in simulation to reality Gap so the testing environment and training environment are differ more and as you can see when there is no simulation to reality you have the performance are similar but when we introduce simulation to reality yeah the performance of the non-robos fqa algorithm is falling quite sharp uh past uh but the performance of our robust fql algorithm RFQ algorithm remains more or less the same okay as intended that's the idea of professional we did this evaluation in a number of other environment we also compare with the td3 algorithm like a state of the art uh policy gradient algorithm I think the performance will be better appreciated in the in this simulation video okay so here in the uh first video is the performance of td3 algorithm on the nominal model so as expected it is performing well there is no simulation to reality Gap the second video shows that td3 performance uh when we introduce some simulation to reality Yeah by changing the control actuator range as you can see when there is simulation reality Gap the standard uh policy gradient algorithm is failing but the third video is essentially our algorithm in the presence of the same simulation to reality yeah and we you can see that that is working and we essentially compare this with other algorithm for example fq algorithm same story the first video the standard non robust algorithm with no simulation reality Gap working second video when we introduce the simulation to reality Gap standard algorithm fills our algorithm will work in the presence of simulation to reality yeah for fun we actually tried this by changing the Gravity by 20 percentage so you can see the first video is fqi algorithm it is not able to work not able to Hope in the reduced gravity environment second is TDC algorithm is trying to open that reduced gravity environment our algorithm you can see that it is working and you can see the reduced effect of gravity in that kind of moonwalk kind of jumps yes policy means that like you you there is no real world a real-time adaptation I have just one policy and that is being applied okay so um very quickly I will um quickly talk about this recent work uh where I got this great opportunity to work on a real world application and demonstration of a robustrial algorithm this is a join work with a Crosshair mobile Benedict from the aerospace engineering department at Texas A M University also it's a two brilliant students Vishnu Vishnu as a master student in my department and bothan was a PhD student in the aerospace engineering department the processor mobile Dynamic is a an aerospace engineer he builds things and fly things like drone and helicopters he's a real engineer unlike me I prefer more theorems proof and algorithm he builds things and fly things so for him an upper bond does not characterize as a valid proof of an algorithm even if it matches with the lower bond for him an upper bound Works uh when he's able to or a theorem for an algorithm works when he's able to use that algorithm in his application and make drones flap so uh some couple of years back he told I think around two years back one and a half years back he told me that he's working on an algorithm he want to develop an algorithm for autonomous ship Landing for vertical takeoff and Landing capable uavs using only a monocular camera in the UAV for tracking and localization he told me that it's a very challenging problem because of the small Landing Space six degrees of freedom should ship that motion limited visual reference for localization and adversarial wind gust and he tried um some standard algorithms like uh pad controller or like modified lqr which did not really work so he was we are discussing this problem and I asked him can we use robust Arrow to solve this problem and he said yes let's try and that was the beginning of this project this started before I started working on the RFQ algorithm so at that time we used a different robustness criteria and different algorithmic approach to solve this problem so as I mentioned rmdp solves a maximum problem which can be conservative in some scenarios so we at that time used a relaxed robustness criteria so in particular we use this soft robust objective where the mean overall possible model is replaced by expectation over all possible model a software objective so this objective is uh known in the robotics plus r literature also which is called domain randomization I think many of you may be familiar with domain randomization uh to those who are not the domain randomization is a symbol in here idea essentially the idea is to randomize the simulation model parameters during training so the policy or the neural network which which is uh neural network which represent the policy will be exposed to a wide range of environments and training and this policy train in the simulation will generalize to real world with a no additional training okay so essentially it's an intuitive or heuristic approach to overcome the simulation reality yeah so we decided to follow this approach and we developed a soft robust RL algorithm using only simulator training considering many robustification and then we tested it on the real world setting without any additional fine tuning the students actually build this platform this is like a model of a ship deck platform the green bar is the uh Horizon bar which we use to localize the Drone we used a big fan to kind of create the winch I think the rather than telling you how it worked it will be better I show the video for those who are in the zoom are you able to see the full screen or the partial screen okay great Okay so so this is the base scenario you can see the movement of the platform this is a simple PID controller when there is no winch and it is working well then we created this diagonal Windrush you can see a big fan the effect of wind in this um protein cover and it's trying to the PID controller is trying to land but it's not working and our robust Arrow control algorithm is able to fight off that winch and then land on the platform so the effect is a little more dramatic when I have this orthogonal vintage you see that that standard pad control algorithm is not able to control the Drone and it's kind of got flown away and the soft rubber startled algorithm that we developed possible to fight off that wind you can see that effect of like it's not very smooth but it's fighting off the wind inch by inch but not losing the control and going and landing in that platform yeah same rubbish policy did you train this without wind so this Robert so So Soft so as I mentioned this is uh soft rubbish criteria so we randomize the environment so during the training we tried wins um and remember exactly so um so we tried by yeah don't exactly remember maybe one two four per second or something in a simulator yeah so everything is done on the simulator and then uh this is tested in the real world without any additional fine tunes okay so um in the remaining time so give me a five minutes warning when that's ready okay so great so in the remaining time I will quickly tell about my work on safe reinforcement learning uh we actually have a series of work on the topic of safe reinforcement learning uh today I will focus mainly on this first paper which was presented in Europe's um two months back so for this multiple papers I got the wonderful opportunity to collaborate with uh many PhD students and my colleagues in the ECE department at Texas A M University archana Arya rohita and sapna are the PhD students and srinivas JF Kumar and Chavo are the professors in the EC Department so just like us I asked the question I mean I asked this question myself when I was working on Robert starrell why do I need to work on progress travel what is the motivation for robustar in the same way why do we need safe reimbursement learning but I think here the pro maybe it may be a little more intuitive because the control policies for any real world engineering system should maintain some necessary safety criteria to avoid undesirable outcome we want to avoid Coalition in autonomous vehicle we want the robot to fall down we want don't want the robot to fall down we want to avoid blackouts in Power Systems but reinforcement learning algorithm May violate the necessary safety constraints during learning and during deployment and the safety constraint violation may lead to catastrophic outcome in real-world Engineering Systems so how do we develop reinforcement learning algorithm that find the optimal policy without violating the safety constraints during learning and during deployment this is the question that I have tried to address in a number of words so just like I used robust mdp formulation to address that intuity problem here I am going to use the formalism of constrained Markov decision process this is a standard formalism now used in the literature to model safe reinforcement learning problem here we model the safety requirements as constraints on the cumulative cost so in a cmdp there is the same state space action space and the Horizon Edge so here I am going to consider like an episodic cmdp not the infinite Horizon discounted reward I am considering because I want to characterize the safety during learning different from the standard mdp semdp has two reward function or cost function here we have an objective reward function R and a constraint cost function c and a safety constrained c bar so the value function value of a policy Pi for a reward function or cost function can be defined in the following way that is nothing but the expected cumulative cost or expected cumulative uh reward so in cmdp unlike the standard mdp we have two value function an objective value function with respect to the objective reward function R and A constrained value function with respect to the constrained cost function C and the cmdp problem is to find the optimal safe policy Pi star which maximize the objective value function thus that the constraint value function is less than Oracle c bar okay so safe reinforcement learning or constrained reinforcement learning problem is a learning version of the cmdp problem here our goal is to learn the optimal safe policy when the model is unknown but without violating the safety constraints during learning so to bring out this idea I focus on an episodic online learning setting that is the algorithm select a learning policy in each learning episode it's a finite Horizon problem so in each learning for each in each learning episode okay the algorithm selects a policy Pi k then the pi K is used to collect the data that is State action reward and the next state the data and the algorithm use the data to update the policy the goal is to converse to the optimal safe policy but in safe reinforcement learning we have the additional requirement that each of this policy in the sequence should satisfy the safety constraints so the constraint value function for the policy Pi case should be less than or equal to c bar for all k so those who are familiar with the exploration exploitation ideas you can immediately see that this online learning problem now is closely connected with the exploration exploitation problem in RL but here you have an additional constraint that that during exploration the policies should not violate the constraints okay so to bring out that idea I Define two regret Notions in safe reinforcement learning here we have the object you regret which is similar to The Standard regret so inside the summation this is the difference between the objective value in function corresponding to Pi star and the objective value function corresponding to the learning policy Pi K in the episode K constrained that is nothing but the cumulative constraint valuation and our goal is the following can we develop a learning algorithm which will minimize the object you regret while keeping the constrained to zero excuse me so so let me quickly summarize the related work in the context of cmdp and constraint bandits in this slide so there has been some very interesting works on exploration in constrained Bandits how do you learn um kind of an optimal arm or optimal linear banded prop solid and linear banded problem given that you have constraint and you don't want to violate the constraint violation from Amani atal gas and from layings group here and in fact we actually used one of the proof idea from this paper to solve like a cmdp problem in our one of our present pre-converter so in the state reinforcement learning in RL can be divided the works in safe reinforcement learning um can be divided into two categories there's a number of work which is trying to learn the optimal safe policy but they allow constraint violation during the learning so this bullet point essentially shows some of the works in that domain including some works on my groups the other classes where I am focusing were exploration in constrain oral constrain RL without violating the safety constraints I would like to emphasize that again here processor layings group has a almost concurrent work with our uh the second work that we have here which I am presenting here your algorithm is called triple Q as I am sure the students are here or in watching Zoom so again I'm assuming um acknowledge that it's only a partial list uh here also I'm focusing on cmdp formulation say farl is a broader broad area there may be other formulation apologies if I'm missing anything important okay so um classical rigorous minimization algorithm in Bandits and RL follow this idea of optimism in the case of uncertainty so the first thing that we asked ourselves is can we use of algorithms to solve the safe reinforcement learning problem so what is ofp algorithm or if your algorithm is essentially you can formulate it as a model based episodic learning algorithm so in each learning episode we construct the estimate the model P hat K and we construct an uncertainty set script p k around the estimate P hat K so here beta is the radius of the uncertainty set that's of the order of one hour square root of n k of sa so NK of s is the number of times the algorithm has seen sampled from the state action pair essay until the episode small K the exact form of the radius is not important because that's to apply the one chain inequality but important thing is that is of the order of one over square root of NK then in the ofp approaches uh we find the policy by solving this optimization problem so here we select an optimistic model from the uncertainty such and we find the optimal policy for that optimistic model so PK is the optimistic model and Pi K is the optimal policy for that optimistic model and this repeats and in most standard Bandits and RL algorithm a variant of this approach will give you like an optimal solution which will minimize the regret but intuitively itself it's kind of um it's kind of intuitive that this new or if you approach may not solve our safe reinforcement learning problem why because we are being optimistic with respect to the model we are not considering the constraint at all so being optimistic with respect is a model May violate the constraint okay and in fact this was shown in another work where they showed that the neoipu style algorithm will get you the order of square root K object you regret which is good because that's order optimal regress for the objective regret but the constraint violation regret will also be order of square root K which is bad which essentially means that you are keeping on violating the constraints so our question was the following can we get an order of square root K objective regret while achieving zero constraint requests so how do we solve this problem so we asked ourselves why is the name of algorithm is failing as I just mentioned it is because it is optimistic with respect to the model it's not only optimistic with Russell as a model it is not considering the constraint at all so it is optimistic with respect to the constraint also then we had a simple solution to overcome this hey it is violating the constraint because it is optimistic with respect to the constraint so let's be pessimistic with respect to the constraint so we thought that okay if you avoid the constraint violation by R being pessimistic with respect to the constraint can we solve this problem so we did that by considering a pessimistic cost function c bar so c bar is C the standard cost function plus a pessimistic term so you can see that the pessimistic term is proportional to the beta the radius of uncertainty said will this approach work it will work in one way that is it will work and it will not violate the constraint but it will not work because your objective regret will be very bad that is if you use this approach in the of if you use the pessimistic cost function in the ofu you may not violate the constraint but you won't get any reasonable performance so it's very pessimistic so we have a pessimistic solution but it's very pessimistic so how do we overcome this problem so we thought that okay we are very being very pessimistic let's introduce some more optimism so we achieved that by the double optimism with respect to the reward function so we created an optimistic reward function R Bar which is the standard reward function plus an optimism term you can see that optimism is also proportional the radius of uncertainty set but with a different constant then by conveying this we proposed our algorithm so our algorithm is looks similar to the ofu approach but it is different in the following way instead of the standard reward function we have an optimistic reward function instead of the standard cost function we have a pessimistic cost function and in addition we have the standard optimism with respect to the model so we have two optimism one optimism with respect to the model and another optimism with respect to the reward and pessimism with respect to the cost function so we called our algorithm little bit cheekly uh doubly optimistic pessimistic exploration algorithm Dot and the solution is that solve this problem I think learning episode and then use that learning algorithm in that learning episode so one thing very clear before starting this problem itself is that intuitively it is not possible to guarantee safety in the beginning of the learning if you do not know anything about the model okay so one standard assumption used in the literature is that you assume that there is you know a safe Baseline policy that's not the optimal policy but a safe Baseline policy and you can start with the safe Baseline policy so we use the same approach our algorithm uses a safe Baseline policy for the first K naught episode that K naught does not depend on the total number of episode capital K it's a constant we can give a precise characterization of the constant and our algorithm is the following first K naught episode use a safe Baseline policy then use the dot policy so we can show that this algorithm achieves the desired results excuse me so under the drop algorithm of course with high probability only we can achieve zero constrained regret that is the learning policy selected by the algorithm do not violate the constraints during that learning and at the same time you get almost odd optimal regret almost optimal because it is of the order of square root K and I believe it's order optimal with respect to action I think you can save a little bit on the state and Horizon uh that's why it's almost optimal okay that depends on Delta so the Delta is one over Delta that c c bar by c Bar B is the Delta that automatically so in fact this dop algorithm is actually an improvement on a previous work from um our group where we kind of saved some regret Factor also it performs much much better in the simulation performance okay so I'm not going to spend time on the simulation performance here due to time constraint Now dog gives flowable guarantees but as you have seen it's like a tabular model based episodic learning algorithm but it does not really scale to large State space now the question is that can we develop scalable reinforcement learning safely enforcement learning algorithm with screwable guarantees in other words can be developed in our contest can we develop policy gradient algorithms for constrained Markov decision process so from our group we actually have a work on past converging policy gradient algorithm for cmdp which is in the archive then we realize that actually that methodology can be used to solve a class or problem more generally characterize multi-objective reinforcement learning why this multi-objective for example cmdp can be thought as multi-objective you are maximizing one objective such that the other objective is less than or as a constraint you can actually think of a non-linear combination of multiple objective for example if you want to ensure some kind of alpha fairness log utility fairness so you can think about like a log sum of the value function and for those problems there is no dynamic programming approach so you need to use optimization approach so can we solve that class or problem like policy gradient a recent work from us uh in the new routes essentially solve that problem and then cmdp will become a special case of that and we can show that we can get a global convergence rate of the order of one over t I think Okay so to summarize um so I presented uh some part of the work from our group on robust turtle and say parallel today so as I mentioned why I wanted to work on this work was that I believe that simulation to reality Gap is a very important problem that we need to solve to make RL algorithm Deployable in the real world engineering system and in addition to the simulation to reality gap which is essentially considering the model mismatch safety is another important factor that we need to consider so and again since I am more on the theoretical and algorithmic side I would like to formulate this from this mathematical methods and try to get throughable guarantees for large State space problem so for the robust RL I use the framework of robust mdp and robust Turtle to solve this for large State space for CM for the say final problem I use the formulation of cmdp solve this problem and we have like a lot of interesting future possible directions here we are currently developing some policy gradient algorithm for robust Turtle I'm also generally interested in very much interested in more fundamental questions like how do you develop stochastic approximational algorithms like TD learning Q learning um or robustrial algorithm for given the uncertainty set and then I am interested in the robust and meta oral algorithm so as you know meta RL idea essentially meta learning idea indicates the ability to adapt in the real world robust person and why why do you want to adopt the real world to overcome the simulation reality Gap robustness is also overcoming the simulation to reality Gap but being robust or pessimistic so it's too extreme right so is there a sweet spots between that you may not be able to completely adapt in the real world there is some pre-training is required and then you might start you might want to start conservatively so where is the seed spot how do you combine this robust approach and meta oral approach to come up with a better algorithm so that is something that we are working on now so with that I will conclude uh thank you so much for listening and I'll be happy to take any questions foreign 